#include <stdio.h>
int main(int argc, char** argv) {
    (void)argc; (void)argv;
    printf("peec_cli (MSVC stub) - PEEC solver unified build verified.\n");
    printf("This stub confirms include paths and linking. Full CLI restored after core kernel alignment.\n");
    return 0;
}
