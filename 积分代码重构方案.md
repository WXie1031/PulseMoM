# 积分代码重构方案

## 当前问题总结

### ❌ 问题1：公共积分函数实现过于简化

**位置：** `src/core/electromagnetic_kernels.c`

```c
// 第142行 - 只是简单调用Green函数，没有真正的数值积分
complex_t integrate_triangle_singular(const triangle_element_t* t,const double* p,double k,integral_kernel_t kt){ 
    (void)p;(void)kt; 
    double r=sqrt(fmax(t->area,0.0)); 
    return green_function_free_space(r,k);  // ❌ 太简化
}

// 第146行 - 同样过于简化
complex_t integrate_wire_thin(const wire_element_t* w,const double* p,double k,integral_kernel_t kt){ 
    (void)p;(void)kt; 
    double x=fmax(w->length,w->radius); 
    return green_function_free_space(x,k);  // ❌ 太简化
}
```

**问题：** 这些函数没有实现真正的数值积分，只是简单的距离计算。

### ❌ 问题2：MoM未使用公共积分函数

**位置：** `src/core/core_assembler.c`

```c
// 第225-229行 - 空实现
static complex_t integrate_triangle_triangle(...) {
    (void)tri_i; (void)tri_j; (void)frequency; (void)formulation; 
    complex_t z={0.0,0.0}; return z;  // ❌ 空实现
}

// 第300-316行 - 有转换代码但未调用
static complex_t kernel_self_term_triangle(...) {
    triangle_element_t tri_elem;
    // 转换几何结构...
    // Use the electromagnetic_kernels integration function
    (void)formulation; (void)k; return (complex_t){0.0, 0.0};  // ❌ 未调用
}
```

**问题：** 虽然有调用公共函数的意图，但实际是空实现。

### ❌ 问题3：PEEC完全独立实现

**位置：** `src/solvers/peec/peec_integration.c`

```c
// 第41-70行 - 自己实现高斯积分
static void gauss_quadrature_1d(int n, double* points, double* weights) {
    // 完整的高斯积分实现...
}

// 第90-150行 - 自己实现Neumann公式积分
static peec_complex_t compute_partial_inductance_neumann_integral(...) {
    // 完整的双重线积分实现...
}
```

**问题：** 
- 重复实现了高斯积分（`core_assembler.c`中也有）
- 没有使用公共的Green函数
- 没有使用公共的奇异性处理

### ❌ 问题4：代码重复

1. **高斯积分**：在 `peec_integration.c` 和 `core_assembler.c` 中都有实现
2. **几何插值**：在多个地方重复实现
3. **Green函数**：可能在不同地方重复计算

## 重构方案

### 方案：统一使用公共积分库（推荐）

#### 步骤1：增强公共积分函数库

**文件：** `src/core/electromagnetic_kernels.c`

需要实现：

1. **真正的数值积分**（替换简化实现）
2. **双重积分**（PEEC需要）
3. **Neumann公式积分**（PEEC部分电感）
4. **统一的奇异性处理**

#### 步骤2：创建统一的积分接口

**文件：** `src/core/integration_unified.h` (新建)

```c
// 统一的积分接口，供MoM和PEEC使用

// 双重面积分（PEEC部分电容）
double integrate_surface_surface_capacitance(
    const triangle_element_t *surf_i,
    const triangle_element_t *surf_j,
    double k);

// 双重面积分（PEEC部分电感）
complex_t integrate_surface_surface_inductance(
    const triangle_element_t *surf_i,
    const triangle_element_t *surf_j,
    double k);

// Neumann公式积分（PEEC部分电感）
complex_t integrate_wire_wire_neumann(
    const wire_element_t *wire_i,
    const wire_element_t *wire_j,
    double k);

// 四边形双重积分
complex_t integrate_quad_quad_double(
    const rectangle_element_t *quad_i,
    const rectangle_element_t *quad_j,
    double k);
```

#### 步骤3：重构PEEC积分代码

**修改：** `src/solvers/peec/peec_integration.c`

```c
// 移除自己的高斯积分实现，使用公共函数
// 移除自己的Neumann积分实现，调用公共函数

peec_complex_t peec_compute_partial_inductance_wire_integral(...) {
    // 转换几何结构
    wire_element_t wire1, wire2;
    convert_peec_wire_to_wire_element(..., &wire1);
    convert_peec_wire_to_wire_element(..., &wire2);
    
    // 调用公共积分函数
    complex_t result = integrate_wire_wire_neumann(&wire1, &wire2, k);
    
    // 转换结果
    return convert_complex_to_peec_complex(result);
}
```

#### 步骤4：重构MoM积分代码

**修改：** `src/core/core_assembler.c`

```c
static complex_t integrate_triangle_triangle(...) {
    // 转换几何结构
    triangle_element_t tri_i_elem, tri_j_elem;
    convert_geom_triangle_to_triangle_element(tri_i, &tri_i_elem);
    convert_geom_triangle_to_triangle_element(tri_j, &tri_j_elem);
    
    // 调用公共积分函数
    double obs_point[3] = {tri_j_elem.vertices[0][0], ...};
    return integrate_triangle_singular(&tri_i_elem, obs_point, k, KERNEL_G);
}
```

## 实施优先级

### 高优先级（立即实施）

1. ✅ **增强公共积分函数** - 实现真正的数值积分
2. ✅ **实现双重积分** - PEEC必需
3. ✅ **实现Neumann公式** - PEEC部分电感必需

### 中优先级（后续实施）

4. ⚠️ **重构PEEC代码** - 使用公共函数
5. ⚠️ **重构MoM代码** - 使用公共函数
6. ⚠️ **移除重复代码** - 统一使用公共实现

### 低优先级（优化）

7. ⚙️ **性能优化** - 缓存、并行化
8. ⚙️ **精度优化** - 自适应积分

## 代码组织目标架构

```
┌─────────────────────────────────────────┐
│  MoM Solver                              │
│  - mom_solver_unified.c                  │
│  - 调用统一积分接口                        │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  PEEC Solver                              │
│  - peec_solver_unified.c                 │
│  - peec_integration.c (调用公共函数)      │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  Unified Integration Interface           │
│  - integration_unified.h                 │
│  - 统一的积分接口层                       │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  Core Integration Library                │
│  - electromagnetic_kernels.c             │
│  - 高斯积分、奇异性处理、Green函数          │
│  - 所有数值积分实现                        │
└─────────────────────────────────────────┘
```

## 检查清单

### 当前状态

- [ ] MoM是否调用公共积分函数？ **否** - 占位符未实现
- [ ] PEEC是否调用公共积分函数？ **否** - 独立实现
- [ ] 公共积分函数是否完整？ **否** - 实现过于简化
- [ ] 是否有代码重复？ **是** - 高斯积分等重复
- [ ] 奇异性处理是否统一？ **否** - 各自处理

### 目标状态

- [ ] MoM调用公共积分函数
- [ ] PEEC调用公共积分函数
- [ ] 公共积分函数完整实现
- [ ] 无代码重复
- [ ] 统一的奇异性处理

## 结论

**现有代码没有按照公用积分来设计。**

需要立即重构：
1. 增强公共积分函数库，实现真正的数值积分
2. 添加PEEC需要的双重积分和Neumann公式
3. 重构PEEC和MoM代码，统一使用公共积分函数
4. 移除重复实现，统一代码组织

