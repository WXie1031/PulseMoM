## 目标
- 在保持 MSVC 兼容与现有构建稳定的前提下，补全层状介质 Green 函数的连续谱主贡献（同层/跨层），并建立可验证的数值校核与性能路径。

## 技术路线
- 连续谱近似：先实现不含极点/分支切线的主值积分近似（径向频率 kρ 的有限区间采样 + 收敛权重），确保数值稳定。
- 边界条件与传输：保留已实现的界面传输近似 |T|，在同层与跨层路径中合并使用，逐步替换为更准确的界面系数（支持 TE/TM 分解）。
- 衰减与稳定：对高导电层使用皮肤深度 δ 的衰减；对积分核应用裁剪与自适应步长，避免数值爆炸。

## 接口与类型
- 继续使用 `complex_t` 与 C 接口（`electromagnetic_kernels.h`），不引入 C 复数；所有新增函数维持现有命名与参数风格。
- `green_function_layered_media` 内部拆分为同层/跨层两个路径，实现独立近似后再汇合。

## 测试与验证
- CLI 扩展：在 `mom_cli` 增加参数化扫描（σ、|Z|、厚度、z/z'、ρ），打印相对误差与趋势；保留 Stub 模式不依赖求解器与网格 API。
- 单元测试：在 `tests` 新增最小测试（双层介质、固定 k0），对 Re/Im 与梯度进行阈值校验；构建目标仅在 MSVC 下启用这些测试。
- 一致性：保留自由空间核的有限差分对比，作为回归基线。

## 性能与并行
- 先串行实现主值近似，加入分批评估接口（已存在 batch API）；如有必要再引入 OpenMP 标准 for 初始化（MSVC 可解析）。
- 为大规模评估增加插值表（kρ 采样），并提供缓存复用。

## 渐进启用模块
- 在 MSVC 下逐步恢复被排除的核心源文件（如 mesh/assembler），每一步确保：类型一致（`complex_t`）、无不兼容语法、目标级构建与测试通过。

## 风险与回退
- 若连续谱近似在部分参数下不稳定：回退到自由空间 + 近似传输系数与衰减；保留接口不变，便于后续替换。
- 对任何新增接口，提供禁用宏与安全默认值，避免影响现有用户路径。

## 交付与里程碑
- 里程碑 1：同层主值近似与 CLI 校核通过（构建 0 错误）。
- 里程碑 2：跨层主值近似与插值表缓存；批处理评估通过。
- 里程碑 3：参数化测试覆盖与性能小结；准备逐步恢复更多模块。

请确认以上计划，我将按该方案实施并在每个里程碑完成后进行构建与数值校核汇报。